<%= form_for(@bug, url: url) do |f| %>
  <% if @bug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug.errors.count, "error") %> prohibited this bug from being saved:</h2>

      <ul>
      <% @bug.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :bug_status_id %><br />
    <%= f.select :bug_status_id, bug_status_select_options %>
  </div>
  <div class="actions">
    <%= f.submit %><%= link_to "cancel", project_path(@project), class: "cancel" %>
  </div>
<% end %>
